<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{get_emoji}}} {{story.user.username}}</h2>
    {{story.title}}
    {{story.content}}
    {{story.image}}
    <div>
    {{#each story.tags}}
    {{name}}
    {{/each}}
    </div>
    <div>
    {{#each story.comment}}
    {{description}}
    {{/each}}
    </div>
</div>
 